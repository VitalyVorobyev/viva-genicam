# genapi-xml

Async helpers for downloading and parsing GenICam XML node maps.
It exposes strongly typed schema fragments and utilities for feature lookup.

```bash
cargo add genapi-xml
```

```rust
use viva_genapi_xml::Client;
```

See the [workspace README](../../README.md) for roadmap details and design context.
